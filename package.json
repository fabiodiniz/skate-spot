{
  "name": "skate-spot",
  "version": "0.0.1",
  "author": "Fábio Diniz <contato@fabiodiniz.com.br>",
  "license": "Copyright Holder",
  "scripts": {
    "heroku-postbuild": "cd backend && yarn install && yarn global add @nestjs/cli && yarn run build",
    "start": "cd backend && yarn run start:prod"
  },
  "engines": {
    "node": "^16.14"
  },
  "devDependencies": {
    "@types/node": "^17.0.21"
  }
}
